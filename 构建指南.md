# 抖音时间管控 - 构建指南

## 当前状态
✅ 代码已完成，已提交到本地 Git 仓库
❌ 尚未推送到 GitHub（需要你操作）
❌ 尚未编译 APK（需要 Android SDK 或 GitHub Actions）

---

## 方式一：使用 GitHub Actions 云端构建（推荐）

### 第一步：创建 GitHub 仓库
1. 打开 https://github.com/new
2. 仓库名：`douyin-timer-control`
3. 选择 **Public**（免费，任何人都能看到）
4. 勾选 **Add a README file**（可选）
5. 点击 **Create repository**
6. 复制仓库 URL，例如：`https://github.com/你的用户名/douyin-timer-control.git`

### 第二步：推送代码
在你的电脑上打开 PowerShell 或命令提示符，执行：

```bash
cd "C:\Users\jams\WorkBuddy\2026-08-29-14-18-26\douyin-timer-app"
git remote add origin https://github.com/你的用户名/douyin-timer-control.git
git branch -M main
git push -u origin main
```

### 第三步：触发构建
推送完成后，GitHub Actions 会自动开始构建。
- 进入仓库 → **Actions** 标签
- 等待构建完成（约 3-5 分钟）
- 点击最右边的运行记录 → **Artifacts** 下载 APK

---

## 方式二：本地 Android Studio 构建

### 第一步：安装 Android Studio
下载：https://developer.android.google.cn/studio

### 第二步：打开项目
1. 打开 Android Studio
2. 点击 **Open**
3. 选择目录：`C:\Users\jams\WorkBuddy\2026-08-29-14-18-26\douyin-timer-app\android`
4. 等待 Gradle 同步完成

### 第三步：连接手机并运行
1. 用 USB 连接 Android 手机（Android 8.0+）
2. 手机开启 **开发者模式** 和 **USB 调试**
3. 在 Android Studio 点击 **Run** 按钮（绿色三角）
4. 选择你的手机设备
5. 等待编译安装完成

### 第四步：导出 APK
1. 菜单 **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
2. APK 位置：`app\build\outputs\apk\debug\app-debug.apk`

---

## 首次使用权限设置

安装后，需要授权以下权限：

### 1. 无障碍服务
```
手机设置 → 无障碍 → 抖音管控服务 → 开启
```

### 2. 悬浮窗权限
```
手机设置 → 应用 → 抖音管控 → 权限 → 悬浮窗 → 允许
```

### 3. 后台弹出界面（部分机型需要）
```
手机设置 → 应用 → 抖音管控 → 权限 → 后台弹出界面 → 允许
```

---

## 使用说明

1. 打开 App
2. 点击 **开始管控**
3. 每 1 小时有 15 分钟可用时间（按自然小时计算）
4. 超过 15 分钟后，抖音会自动被拦截
5. 点击 **重置定时** 可以重置当前周期

---

## 项目文件位置

```
C:\Users\jams\WorkBuddy\2026-08-29-14-18-26\douyin-timer-app\
├── android/              # Android 项目
│   ├── app/src/main/java/com/example/douyincontrol/
│   │   ├── MainActivity.kt          # 主界面
│   │   ├── TimerService.kt          # 定时服务
│   │   └── AccessibilityControlService.kt  # 无障碍服务
│   ├── app/src/main/res/            # 资源文件
│   └── build.gradle                 # 构建配置
├── .github/workflows/build.yml      # GitHub Actions 配置
├── README.md                        # 项目说明
└── 构建指南.md                      # 本文档
```
